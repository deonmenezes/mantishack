# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false

# Refuse packages published less than 4 days ago. Mitigates supply-chain
# attacks where a compromised maintainer token uploads a malicious
# version; community detection + npm unpublish typically lands inside
# 48 hours. See `minimumReleaseAge` (minutes) in package.json.
